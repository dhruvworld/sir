[build]
  base = "."
  command = "npm install && cd frontend && npm install && npm run build"
  publish = "frontend/dist"

[functions]
  directory = "netlify/functions"

[functions.search]
  included_files = ["data/city_route.json"]

[functions.meta]
  included_files = ["data/city_route.json"]

